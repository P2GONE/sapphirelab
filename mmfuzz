#!/usr/bin/env bash
# Convenience launcher for the multimodal jailbreak fuzzer.
# Forwards every argument to mmfuzz.py with the script's dir on PYTHONPATH.
set -e
DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "$DIR/mmfuzz.py" "$@"
