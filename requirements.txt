# Core HTTP / config / banner deps. Loose lower bounds so installs work on
# Python 3.10 .. 3.13 without forcing a 2023 snapshot.
certifi>=2023.5.7
charset-normalizer>=3.1.0
idna>=3.4
pyfiglet>=0.8.post1
PyYAML>=6.0
requests>=2.30.0
termcolor>=2.3.0
urllib3>=2.0.2

# Multimodal fuzzer (mmfuzz.py)
Pillow>=10.0.0
gTTS>=2.4.0
pydub>=0.25.1
moviepy>=1.0.3      # video modality
# matplotlib>=3.5    # optional: enables system-wide font discovery in fuzzer/_fonts.py

# Mutation providers — install ONLY the one you actually use:
#   pip install anthropic          # for Mutation.Provider: anthropic
#   pip install openai             # for Mutation.Provider: openai
#   pip install google-genai       # for Mutation.Provider: gemini
