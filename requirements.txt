# ── Core (text fuzzer) ────────────────────────────────────────────────────────
certifi==2023.5.7
charset-normalizer==3.1.0
idna==3.4
pyfiglet==0.8.post1
PyYAML==6.0
requests==2.30.0
termcolor==2.3.0
urllib3==2.0.2

# ── Image processing (image fuzzer) ──────────────────────────────────────────
Pillow>=10.0.0
numpy>=1.24.0
piexif>=1.1.3

# ── Gemini API (image runner + text mutation) ─────────────────────────────────
google-generativeai>=0.8.0

# ── Other LLM providers (text mutation — optional) ───────────────────────────
# anthropic>=0.40.0
# openai>=1.0.0

# ── Config / settings ─────────────────────────────────────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0

# ── Audio processing (audio fuzzer) ──────────────────────────────────────────
scipy>=1.12
soundfile>=0.12
librosa>=0.10
torch>=2.2
torchaudio>=2.2

# ── Async / reporting ─────────────────────────────────────────────────────────
aiohttp>=3.9.0
aiofiles>=23.0.0
rich>=13.0.0
